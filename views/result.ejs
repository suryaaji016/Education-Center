<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Hasil Quiz</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex flex-column min-vh-100">

  <nav class="navbar navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/">Education Center</a>
      <div>
        <span class="text-white me-3">Student</span>
        <a href="/logout" class="btn btn-sm btn-danger">Logout</a>
      </div>
    </div>
  </nav>


  <div class="container my-5">
    <div class="card shadow-lg">
      <div class="card-body text-center">
        <h2 class="fw-bold mb-3"> Hasil Quiz</h2>
        <p class="fs-5">
          <strong>Score:</strong> 
          <span class="<%= score > 0 ? 'text-success' : 'text-danger' %>">
            <%= score %> 
          </span>
        </p>

 
        <div class="table-responsive mt-4">
          <table class="table table-bordered align-middle">
            <thead class="table-dark">
              <tr>
                <th>Pertanyaan</th>
                <th>Jawaban Kamu</th>
                <th>Jawaban Benar</th>
                <th>Status</th>
              </tr>
            </thead>
            <tbody>
              <% details.forEach(d => { %>
                <tr>
                  <td><%= d.question %></td>
                  <td><%= d.studentAnswer %></td>
                  <td><%= d.correctAnswer %></td>
                  <td class="text-center">
                    <% if (d.isCorrect) { %>
                      <span class="badge bg-success">Benar</span>
                    <% } else { %>
                      <span class="badge bg-danger">Salah</span>
                    <% } %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>

        <a href="/home/student/<%= userId %>" class="btn btn-primary mt-3"> Kembali ke Home</a>
      </div>
    </div>
  </div>

  <footer class="bg-dark text-white text-center py-3 mt-auto">
    © 2025 Education Center. All rights reserved.
  </footer>

</body>
</html>
